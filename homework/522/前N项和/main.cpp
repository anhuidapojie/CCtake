#include "first_last_sum.h"

int main(int argc,char const * argv[])
{
    int first,last;
    cout<<"请输入第一个数"<<endl;
    cin>>first;
    cout<<"请输入第二个数"<<endl;
    cin>>last;
    first_last_sum(first,last);
    return 0;
}