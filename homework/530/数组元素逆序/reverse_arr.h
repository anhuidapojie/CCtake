#ifndef _REVERSE_ARR_
#define _REVERSE_ARr_
#include <iostream>
using namespace std;

/**
 * @brief 数组元素逆序
 * 
 * @param 用户要求展示的 int 类型数组空间首地址，对应数组名
 * @param 当前数组对应的容量
 * 
 * @return 无返回值
 */
void reverse_arr(int arr[],int capacity);
#endif